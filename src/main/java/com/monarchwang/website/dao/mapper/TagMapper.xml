<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.monarchwang.website.dao.mapper.TagMapper">
    <resultMap id="BaseResultMap" type="com.monarchwang.website.dao.model.Tag">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="CHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="delete_flag" property="deleteFlag" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="last_modified_by" property="lastModifiedBy" jdbcType="CHAR"/>
    </resultMap>


    <select id="queryByName" resultMap="BaseResultMap">
        SELECT *
        FROM tag
        WHERE delete_flag = 0 AND name = #{name}
    </select>

    <select id="selectTagsByPage" resultType="com.monarchwang.website.rest.dto.out.TagDto">
        SELECT
            t.id,
            t.name,
            t.status,
            sum(atr.id) AS articleSum
        FROM tag t
            LEFT JOIN article_tag_relation atr ON t.id = atr.tag_id
                                                  AND t.delete_flag = 0 AND atr.delete_flag = 0 AND t.status != 0
        ORDER BY t.create_time DESC, articleSum DESC
    </select>
</mapper>